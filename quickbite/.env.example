# Copy this to .env and fill in your values
# The Vite proxy in vite.config.js handles /api → http://localhost:5000
# So you don't need VITE_API_URL unless you deploy separately

# Optional: override API base if not using the proxy
# VITE_API_BASE_URL=http://localhost:5000/api/v1
